{
    "signing": {
        "default": {
            "expiry": "{{ certificates_ca_expiry }}"
        },
        "profiles": {
            "kubernetes": {
                "usages": [
                    "signing",
                    "key encipherment",
                    "server auth",
                    "client auth"
                ],
                "expiry": "{{ certificates_ca_expiry }}"
            }
        }
    }
}
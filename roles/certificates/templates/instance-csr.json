{
    "CN": "system:node:{{ item.host }}",
    "key": {
        "algo": "rsa",
        "size": 2048
    },
    "names": [
        {
            "C": "{{ certificates_c }}",
            "L": "{{ certificates_l }}",
            "O": "system:nodes",
            "OU": "{{ certificates_ou }}",
            "ST": "{{ certificates_st }}"
        }
    ]
}